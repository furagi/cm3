= form_for @file_object_type_family do |f|
  - if @file_object_type_family.errors.any?
    #error_explanation
      h2 = "#{pluralize(@file_object_type_family.errors.count, "error")} prohibited this gtf_file_object_type_family from being saved:"
      ul
        - @file_object_type_family.errors.full_messages.each do |message|
          li = message

  .actions = f.submit
